version: "3.8"

services:
  plataforma-cursos:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: plataforma-cursos
    restart: unless-stopped
    ports:
      - "${PORT:-9823}:9823"
    volumes:
      # Path absoluto para dados persistentes (configurado em .env)
      # NUNCA mude DATA_PATH depois de configurar!
      - ${DATA_PATH}/data:/app/data
      - ${DATA_PATH}/uploads:/app/uploads
      - ${DATA_PATH}/backups:/app/backups

      # Volume de cursos - configurado via variável de ambiente
      # Configure COURSES_PATH no arquivo .env
      - ${COURSES_PATH}:${COURSES_INTERNAL_PATH:-/cursos}:ro
    environment:
      - DATABASE_URL=${DATABASE_URL:-sqlite:////app/data/platform_course.sqlite}
      - FLASK_ENV=${FLASK_ENV:-production}
      - PYTHONUNBUFFERED=1
    networks:
      - plataforma-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9823/api/debug/routes"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  watchtower:
    image: containrrr/watchtower
    container_name: watchtower-plataforma
    restart: unless-stopped
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - WATCHTOWER_CLEANUP=true
      - WATCHTOWER_INCLUDE_RESTARTING=true
      - WATCHTOWER_POLL_INTERVAL=3600
      - WATCHTOWER_INCLUDE_STOPPED=true
      - WATCHTOWER_REVIVE_STOPPED=false
    command: plataforma-cursos
    networks:
      - plataforma-network

networks:
  plataforma-network:
    driver: bridge
